"use client";import {useEffect,useState} from "react";type I={id:string;type:string;name:string;poster?:string;releaseInfo?:string};export default function Library(){const [a,setA]=useState<I[]>([]);useEffect(()=>{setA(JSON.parse(localStorage.getItem("drift-library")||"[]"))},[]);return <main><header><a className="brand" href="/">DRIFT</a><a className="nav" href="/addons">Addons</a></header><section className="hero"><span className="eyebrow">YOUR LIBRARY</span><h1>Keep it close.</h1><p>Saved titles stay in this browser.</p></section>{a.length?<div className="grid">{a.map(x=><article className="card" key={x.id}>{x.poster?<img src={x.poster}/>:<div className="posterFallback">DRIFT</div>}<strong>{x.name}</strong><small>{x.releaseInfo||x.type}</small></article>)}</div>:<section className="empty"><h2>Your library is empty</h2><a href="/">Browse addons →</a></section>}</main>}