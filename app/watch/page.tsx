"use client";import {Suspense} from "react";import {useSearchParams} from "next/navigation";function P(){const p=useSearchParams();const u=p.get("url")||"";const t=p.get("title")||"Drift Player";const native=/\.(mp4|webm|ogg|m3u8)(\?|$)/i.test(u);return <main className="watch"><a className="back" href="/">← Drift</a><h1>{t}</h1>{native?<video className="video" controls autoPlay playsInline src={u}/>:<div className="playerNotice"><h2>Stream resolved</h2><p>This transport needs a compatible playback backend.</p><code>{u}</code></div>}</main>}export default function Watch(){return <Suspense fallback={<main className="watch">Loading…</main>}><P/></Suspense>}