import {NextRequest,NextResponse} from "next/server";export async function GET(req:NextRequest){const url=req.nextUrl.searchParams.get("url");if(!url)return NextResponse.json({error:"Missing addon URL"},{status:400});try{const u=new URL(url);if(!["http:","https:"].includes(u.protocol))throw Error("Only HTTP(S) addons are supported by the web client");const r=await fetch(u,{headers:{"user-agent":"Drift/0.1"},cache:"no-store"});if(!r.ok)throw Error("Addon returned HTTP "+r.status);const j=await r.json();return NextResponse.json(j,{headers:{"Cache-Control":"no-store"}})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid addon"},{status:400})}}